package com.smartcontract.template

static def invoiceAmountChange(externalParams, internalParams) {
    def object = new OneForAll()
    return object.invoiceAmountChange(externalParams, internalParams)
}